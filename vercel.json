{
  "buildCommand": "cd presentation-app && npm install && npm run build",
  "outputDirectory": "presentation-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
